import { SiwxMessage } from "@didtools/cacao";
import { AuthUserInformation, AuthOptions, ISiwxAuth, SignedSiwx } from "../types/auth.js";
import { ITezosProvider, IGenericSignerProvider, SupportedChains } from "../types/providers.js";
import { DID } from "dids";
import { DIDSession } from "did-session";
export declare class OrbisTezosAuth implements ISiwxAuth {
    #private;
    orbisAuthId: string;
    chain: SupportedChains;
    constructor(provider: ITezosProvider | IGenericSignerProvider);
    get provider(): IGenericSignerProvider;
    getUserInformation(): Promise<AuthUserInformation>;
    signSiwx(siwx: SiwxMessage): Promise<SignedSiwx>;
    authenticateDid({ siwxOverwrites, params }?: AuthOptions): Promise<{
        did: DID;
        session: DIDSession;
    }>;
}
