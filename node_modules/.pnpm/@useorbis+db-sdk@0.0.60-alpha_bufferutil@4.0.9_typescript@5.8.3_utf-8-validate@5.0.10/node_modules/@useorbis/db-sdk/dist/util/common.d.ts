import { SupportedChains } from "../index.js";
export type DIDAddress = {
    address: string;
    chain: SupportedChains;
};
export declare const didToAddress: (did: string) => DIDAddress | {
    error: string;
};
export declare const didsToAddresses: (dids: Array<string>) => Array<DIDAddress>;
