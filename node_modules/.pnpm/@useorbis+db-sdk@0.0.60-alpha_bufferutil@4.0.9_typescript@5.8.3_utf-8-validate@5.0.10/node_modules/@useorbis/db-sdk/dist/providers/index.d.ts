import { IGenericSignerProvider, SupportedChains, SupportedProvider } from "../types/providers.js";
export declare function normalizeProvider({ provider, chain, }: {
    provider: SupportedProvider;
    chain: SupportedChains;
}): IGenericSignerProvider;
