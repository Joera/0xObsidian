import { TarHeaderFieldType } from './tar-header-field-type';
/**
 * Generalized transformation interface for header fields.
 * Used TarHeader getter/setter functionality.
 */
export interface TarHeaderFieldTransform<T> {
    serialize(input: T, fieldLength: number): Uint8Array;
    deserialize(input: Uint8Array, fieldLength: number, offset: number): T;
}
export declare namespace TarHeaderFieldTransformType {
    const ASCII: TarHeaderFieldTransform<string>;
    const ASCII_PADDED_END: TarHeaderFieldTransform<string>;
    const INTEGER_OCTAL: TarHeaderFieldTransform<number>;
    const INTEGER_OCTAL_TIMESTAMP: TarHeaderFieldTransform<number>;
    function from(fieldType: TarHeaderFieldType): TarHeaderFieldTransform<any> | undefined;
}
