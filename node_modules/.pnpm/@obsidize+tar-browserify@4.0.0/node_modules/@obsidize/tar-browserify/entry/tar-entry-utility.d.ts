import { AsyncUint8Array, AsyncUint8ArraySearchResult } from '../common/async-uint8array';
export declare namespace TarEntryUtility {
    /**
     * Searches through the given AsyncUint8Array for the next USTAR sector,
     * starting at the given offset.
     */
    function findNextUstarSectorAsync(input: AsyncUint8Array, offset?: number): Promise<AsyncUint8ArraySearchResult | null>;
    /**
     * Searches the given input buffer for a USTAR header tar sector, starting at the given offset.
     * Returns -1 if no valid header sector is found.
     */
    function findNextUstarSectorOffset(input: Uint8Array, offset?: number): number;
}
